# PoloCloud non-root Docker Compose stack

services:
  polocloud-init:
    image: "alpine"
    user: "root"
    volumes:
      - "./data:/data"
    entrypoint:
      - "/bin/sh"
      - "-c"
      - |
        mkdir -p /data/polocloud
        chown -R 1000:1000 /data
        chmod -R 770 /data

  polocloud:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: "/data"
    user: "1000:1000"
    volumes:
      - "./data/polocloud:/data"
    ports:
      - "25565:25565"
    stdin_open: true
    tty: true
    init: true
    depends_on:
      polocloud-init:
        condition: "service_completed_successfully"
